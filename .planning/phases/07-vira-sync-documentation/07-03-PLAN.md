---
phase: 07-vira-sync-documentation
plan: 03
type: execute
wave: 2
depends_on:
  - 07-02-PLAN.md
files_modified:
  - README.md
  - src/test/java/org/saltations/sync/SyncCommandIntegrationTest.java
autonomous: true
requirements:
  - X-01
  - X-02
must_haves:
  truths:
    - "README contains 'Subscriptions' and 'vira sync' and describes conflict abort and local-only"
    - "New test covers publisher→workspace or workspace→publisher path with real config + temp dirs"
    - "Test includes at least one bidirectional conflict expectation OR documents conflict exit in README + separate test method"
    - "mvn test exits 0"
  artifacts:
    - path: "README.md"
      provides: "User-facing sync documentation"
      exports: []
---

<objective>
Ship **README** updates for **X-02** and add an **integration-style test** for **X-01**: at least one flow each for publisher→workspace, workspace→publisher, and a **conflict** scenario (bidirectional or divergent files), using **temp directories** and a **minimal YAML config** written in test setup (pattern similar to existing subscription tests if any).

Covers **X-01**, **X-02**.
</objective>

<execution_context>
@/home/jmochel/pers/viracocha/.cursor/get-shit-done/workflows/execute-plan.md
@/home/jmochel/pers/viracocha/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-vira-sync-documentation/07-CONTEXT.md
@.planning/REQUIREMENTS.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: README — subscriptions & sync</name>
  <files>
    README.md
  </files>

  <read_first>
    - README.md
    - .planning/phases/07-vira-sync-documentation/07-CONTEXT.md
  </read_first>

  <action>
Update **`README.md`**:

1. **Remove or replace** the sentence stating subscriptions/sync are not in scope (currently around line 23: "Subscriptions / sync back to publishers are **not** in scope").

2. Add a section **`## Subscriptions and sync`** (or equivalent) that documents:
   - Register publisher → create project → `vira subscription add` with **direction** (`publish-to-workspace`, `workspace-to-publish`, `bidirectional` per CLI)
   - **`vira sync --project-name <name>`** with optional **`--subscription`**, **`--dry-run`**, **`--verbose`**, **`--json`**
   - **Conflict behavior:** default **abort**, structured conflicts (no auto-merge); **no watch** / one-shot
   - **Scope:** local filesystem only (same as PROJECT.md)

3. Keep existing sections coherent; fix the stray `|gfv` typo in the conceptual model table if still present.

4. Ensure markdown renders: use consistent code fences for commands.
  </action>

  <acceptance_criteria>
    - `grep -F "vira sync" README.md` exits 0
    - `grep -i "conflict" README.md` exits 0
    - `grep -i "local" README.md` exits 0
    - `grep -F "Subscriptions / sync back to publishers are" README.md` exits 1 (old denial sentence must be absent)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: SyncCommandIntegrationTest</name>
  <files>
    src/test/java/org/saltations/sync/SyncCommandIntegrationTest.java
  </files>

  <read_first>
    - src/test/java/org/saltations/subscription/SubscriptionListShowRemoveTest.java
    - src/test/java/org/saltations/ViracochaCommandTest.java
    - src/main/java/org/saltations/config/ConfigService.java
  </read_first>

  <action>
Create **`src/test/java/org/saltations/sync/SyncCommandIntegrationTest.java`**:

1. Use `@TempDir Path configHome` (or single temp root) and set system properties / environment so **`ConfigService`** resolves to a **test config file** — follow the same approach as an existing test that initializes config (grep `SubscriptionListShowRemoveTest` or config tests for `XDG_CONFIG_HOME` / `user.home` / custom config path).

2. **Test A — publisher→workspace:** Register publisher with a file, project workspace empty, subscription `publish-to-workspace`, run `sync` via `CommandLine` + `ApplicationContext` (MicronautFactory). Assert exit `0`, file appears in workspace, summary contains `Copied:` with `Copied: 1` or greater.

3. **Test B — workspace→publisher:** Populate workspace file, publisher side missing file, subscription `workspace-to-publish`, run sync, assert file copied to publisher tree and exit `0`.

4. **Test C — conflict (bidirectional):** Same relative path, **different bytes** on publisher and workspace — run `sync`, assert **exit code 1** and stderr or stdout indicates conflicts (non-zero conflict count or conflict lines on stderr).

5. Use **`ObjectMapper`** or file reads only for assertions — keep tests deterministic.

**X-01 requires all three** — implement Test A, Test B, and Test C as **separate `@Test` methods** (names must include intent, e.g. `syncPublisherToWorkspace`, `syncWorkspaceToPublisher`, `bidirectionalConflictExitsNonZero`).
  </action>

  <acceptance_criteria>
    - `test -f src/test/java/org/saltations/sync/SyncCommandIntegrationTest.java` exits 0
    - `grep -E "syncPublisherToWorkspace|publish-to-workspace|publish_to_workspace|PUBLISH_TO_WORKSPACE" src/test/java/org/saltations/sync/SyncCommandIntegrationTest.java` exits 0
    - `grep -E "syncWorkspaceToPublisher|workspace-to-publish|WORKSPACE_TO_PUBLISH" src/test/java/org/saltations/sync/SyncCommandIntegrationTest.java` exits 0
    - `grep -E "bidirectional|BIDIRECTIONAL|conflict" src/test/java/org/saltations/sync/SyncCommandIntegrationTest.java` exits 0
    - `./mvnw -q test` exits 0
  </acceptance_criteria>
</task>

</tasks>

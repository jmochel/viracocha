---
phase: 03-projects-and-mappings
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/org/saltations/model/ViracochaConfig.java
  - src/main/java/org/saltations/model/ProjectEntry.java
  - src/main/java/org/saltations/model/MappingEntry.java
  - src/main/java/org/saltations/ViracochaCommand.java
  - src/main/java/org/saltations/project/ProjectCommand.java
  - src/main/java/org/saltations/project/CreateProjectCommand.java
  - src/main/java/org/saltations/project/ListProjectsCommand.java
  - src/main/java/org/saltations/project/ShowProjectCommand.java
  - src/main/java/org/saltations/project/UnregisterProjectCommand.java
  - src/main/java/org/saltations/project/AddMappingCommand.java
  - src/test/java/org/saltations/model/ViracochaConfigProjectTypedListTest.java
autonomous: true
requirements: []

must_haves:
  truths:
    - "ViracochaConfig.projects is List<ProjectEntry> not List<Object>"
    - "ProjectEntry has name, path, and List<MappingEntry> mappings"
    - "MappingEntry has patternName, workspacePath, Map<String,String> parameters (or equivalent Jackson field names)"
    - "ViracochaCommand.subcommands includes ProjectCommand.class"
    - "mvn test exits 0 after this plan"
  artifacts:
    - path: "src/main/java/org/saltations/model/ProjectEntry.java"
      provides: "Project container POJO"
      exports: ["ProjectEntry"]
    - path: "src/main/java/org/saltations/model/MappingEntry.java"
      provides: "Per-mapping POJO"
      exports: ["MappingEntry"]
    - path: "src/main/java/org/saltations/project/ProjectCommand.java"
      provides: "Group command wiring five leaf subcommands"
      contains: "CreateProjectCommand.class"
---

<objective>
Establish typed `projects` in central config and wire `vira project` with five leaf subcommands as **stubs** (return 0, no business logic). Plans 03-02 and 03-03 replace stubs with full behavior. This plan matches Phase 2 Plan 02-01: model + compile-ready hierarchy + round-trip test so empty `projects: []` YAML stays compatible.

Per `03-CONTEXT.md` D-09, D-10.
</objective>

<execution_context>
@/home/jmochel/pers/viracocha/.cursor/get-shit-done/workflows/execute-plan.md
@/home/jmochel/pers/viracocha/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-projects-and-mappings/03-CONTEXT.md
@.planning/REQUIREMENTS.md
@src/main/java/org/saltations/model/ViracochaConfig.java
@src/main/java/org/saltations/model/PatternEntry.java
@src/main/java/org/saltations/ViracochaCommand.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ProjectEntry, MappingEntry, upgrade ViracochaConfig</name>
  <files>
    src/main/java/org/saltations/model/MappingEntry.java
    src/main/java/org/saltations/model/ProjectEntry.java
    src/main/java/org/saltations/model/ViracochaConfig.java
    src/test/java/org/saltations/model/ViracochaConfigProjectTypedListTest.java
  </files>

  <read_first>
    - src/main/java/org/saltations/model/ViracochaConfig.java
    - src/main/java/org/saltations/model/PatternEntry.java
    - src/test/java/org/saltations/model/ViracochaConfigTest.java (if present — round-trip pattern)
  </read_first>

  <action>
1. Create `MappingEntry.java` in `org.saltations.model` with Lombok `@Data`, `@NoArgsConstructor`, `@AllArgsConstructor`:
   - Fields: `String patternName`, `String workspacePath`, `Map<String, String> parameters` — use `new LinkedHashMap<>()` as default for `parameters` in the field initializer so YAML order is stable.
   - Jackson: empty map must serialize cleanly; no Micronaut `@ConfigurationProperties`.

2. Create `ProjectEntry.java`:
   - Fields: `String name`, `String path`, `List<MappingEntry> mappings` with default `new ArrayList<>()`.

3. Change `ViracochaConfig.projects` from `List<Object>` to `List<ProjectEntry>` with default `new ArrayList<>()`.

4. Add `src/test/java/org/saltations/model/ViracochaConfigProjectTypedListTest.java`: load/save round-trip YAML (or reuse ConfigService pattern from existing tests) with `projects` containing one `ProjectEntry` with one empty-parameters `MappingEntry`; assert deserialized types are `ProjectEntry` / `MappingEntry`.
  </action>

  <acceptance_criteria>
    - `grep "List<ProjectEntry>" src/main/java/org/saltations/model/ViracochaConfig.java` exits 0
    - `mvn -q test` exits 0
    - New test class file exists and contains `@Test`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Wire ProjectCommand and stub leaf commands + ViracochaCommand</name>
  <files>
    src/main/java/org/saltations/ViracochaCommand.java
    src/main/java/org/saltations/project/ProjectCommand.java
    src/main/java/org/saltations/project/CreateProjectCommand.java
    src/main/java/org/saltations/project/ListProjectsCommand.java
    src/main/java/org/saltations/project/ShowProjectCommand.java
    src/main/java/org/saltations/project/UnregisterProjectCommand.java
    src/main/java/org/saltations/project/AddMappingCommand.java
  </files>

  <read_first>
    - src/main/java/org/saltations/publisher/PublisherCommand.java
    - src/main/java/org/saltations/ViracochaCommand.java
  </read_first>

  <action>
1. Create `org.saltations.project.ProjectCommand` as `@Singleton`, `Callable<Integer>`, `@Command(name = "project", description = "Manage projects and mappings.", mixinStandardHelpOptions = true, subcommands = { CreateProjectCommand.class, ListProjectsCommand.class, AddMappingCommand.class, ShowProjectCommand.class, UnregisterProjectCommand.class })`.

2. Create five stub leaf classes in the same package — each `@Singleton`, `Callable<Integer>`, `@Command` with:
   - `CreateProjectCommand`: `name = "create"`, description mentions registering a project (stub body: `return 0`)
   - `ListProjectsCommand`: `name = "list"`
   - `AddMappingCommand`: `name = "add-mapping"` (hyphenated subcommand name per picocli)
   - `ShowProjectCommand`: `name = "show"`
   - `UnregisterProjectCommand`: `name = "unregister"`
   Each stub: `@Override public Integer call() { return 0; }` only.

3. Edit `ViracochaCommand.java`: add `import org.saltations.project.ProjectCommand;` and add `ProjectCommand.class` to `subcommands` array after `PatternCommand.class`.

4. Run `mvn -q test` and fix any Micronaut bean discovery issues (package must be under component scan for `org.saltations`).
  </action>

  <acceptance_criteria>
    - `grep "ProjectCommand.class" src/main/java/org/saltations/ViracochaCommand.java` exits 0
    - `mvn -q -Dtest=ViracochaCommandTest test` exits 0 (or full `mvn test` if that is the project norm)
    - `mvn -q test` exits 0
  </acceptance_criteria>
</task>

</tasks>

## PLANNING COMPLETE

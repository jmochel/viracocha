---
phase: 03-projects-and-mappings
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02-PLAN.md
files_modified:
  - src/main/java/org/saltations/project/AddMappingCommand.java
  - src/main/java/org/saltations/project/ShowProjectCommand.java
  - src/test/java/org/saltations/project/AddMappingAndShowProjectTest.java
autonomous: true
requirements:
  - PROJ-03
  - PROJ-04
  - PROJ-05

must_haves:
  truths:
    - "add-mapping rejects unknown pattern names without saving (PROJ-04)"
    - "show prints workspace path and each mapping with parameters (PROJ-05)"
    - "Repeatable --param key=value populates MappingEntry.parameters (PROJ-03)"
  artifacts:
    - path: "src/main/java/org/saltations/project/AddMappingCommand.java"
      provides: "PROJ-03, PROJ-04"
    - path: "src/main/java/org/saltations/project/ShowProjectCommand.java"
      provides: "PROJ-05"
---

<objective>
Implement `project add-mapping` and `project show`: validate pattern names against `ViracochaConfig.getPatterns()`, store `MappingEntry` instances, and display human-readable and `--json` output per D-04, D-05, D-07, D-10, D-11.
</objective>

<execution_context>
@/home/jmochel/pers/viracocha/.cursor/get-shit-done/workflows/execute-plan.md
@/home/jmochel/pers/viracocha/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-projects-and-mappings/03-CONTEXT.md
@src/main/java/org/saltations/model/MappingEntry.java
@src/main/java/org/saltations/model/PatternEntry.java
@src/main/java/org/saltations/pattern/ShowPatternCommand.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement add-mapping with --param parsing</name>
  <files>
    src/main/java/org/saltations/project/AddMappingCommand.java
  </files>

  <read_first>
    - src/main/java/org/saltations/project/CreateProjectCommand.java
    - src/main/java/org/saltations/model/MappingEntry.java
    - src/main/java/org/saltations/model/PatternEntry.java
  </read_first>

  <action>
1. **AddMappingCommand** (D-01, D-02, D-07, D-10, PROJ-03, PROJ-04):
   - Options: `--project` (required), `--pattern` (required), `--destination` (required)
   - Repeatable `--param`: use `@Option(names = "--param", arity = "0..*") String[] paramPairs` OR accumulate multiple `--param` into a list; each element must be `key=value` (split on first `=` only); empty value after `=` allowed if product decision says so — default: require `=` present
   - Flow: `load()`; find project by name — if missing, stderr `Project '<name>' not found.`, exit 1
   - Check `config.getPatterns().stream().anyMatch(p -> p.getName().equals(pattern))` — if false, stderr: `Pattern '<pattern>' not found.` (or align with Phase 2 not-found phrasing), exit 1, **no save**
   - Append `new MappingEntry(patternName, destination, parsedMap)` to that project's `getMappings()`
   - `save(config)`; success message to stdout

2. Destination: store as provided string (trim leading/trailing whitespace only if you document it in test); use forward slashes or OS-agnostic storage per D-10 discretion — pick one and test it.
  </action>

  <acceptance_criteria>
    - `AddMappingCommand.java` contains `--destination` and `--param`
    - File contains `getPatterns()` and pattern existence check before `save`
    - `mvn -q test` exits 0 after implementation (tests added in Task 2)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement show + integration tests</name>
  <files>
    src/main/java/org/saltations/project/ShowProjectCommand.java
    src/test/java/org/saltations/project/AddMappingAndShowProjectTest.java
  </files>

  <read_first>
    - src/main/java/org/saltations/pattern/ShowPatternCommand.java
    - src/test/java/org/saltations/project/ProjectCommandsTest.java
  </read_first>

  <action>
1. **ShowProjectCommand** (PROJ-05, D-04, D-05):
   - Options: `--name` (required), `--json`
   - Load config; find project — if null, `Project '<name>' not found.`, exit 1
   - Plain text: print `Name:`, `Path:`, then for each mapping print a small block: pattern name, destination, and parameters (e.g. `Parameters: k1=v1, k2=v2` or one line per mapping with clear labels — must match spirit of D-04 multi-line block)
   - JSON mode: serialize whole `ProjectEntry` with `ObjectMapper` as single object one line (like ShowPatternCommand)

2. **Tests** `AddMappingAndShowProjectTest.java` with `@MicronautTest` and `@TempDir`:
   - Register a minimal pattern in config (reuse PatternEntry / pattern register path or construct config programmatically if tests register patterns via command execution)
   - Create project → add-mapping with two `--param` flags → show plain and show `--json` assert output contains pattern name and param values
   - add-mapping with bogus pattern name → exit 1 and config unchanged (reload file or in-memory assert mapping count)
  </action>

  <acceptance_criteria>
    - `ShowProjectCommand.java` contains `--json` and `Name:` and `Path:`
    - Test file contains `add-mapping` and `not found` for bad pattern
    - `mvn -q test` exits 0
  </acceptance_criteria>
</task>

</tasks>

## PLANNING COMPLETE

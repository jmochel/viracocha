---
phase: 03-projects-and-mappings
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01-PLAN.md
files_modified:
  - src/main/java/org/saltations/project/CreateProjectCommand.java
  - src/main/java/org/saltations/project/ListProjectsCommand.java
  - src/main/java/org/saltations/project/UnregisterProjectCommand.java
  - src/test/java/org/saltations/project/ProjectCommandsTest.java
autonomous: true
requirements:
  - PROJ-01
  - PROJ-02
  - PROJ-06

must_haves:
  truths:
    - "`vira project create --name X --path Y` persists ProjectEntry when config initialized"
    - "`vira project list` prints name and path columns (or JSONL with --json)"
    - "`vira project unregister --name X` removes project when present"
    - "Duplicate create and unregister of missing name exit 1 with messages per D-06/D-08"
  artifacts:
    - path: "src/main/java/org/saltations/project/CreateProjectCommand.java"
      provides: "PROJ-01"
    - path: "src/main/java/org/saltations/project/ListProjectsCommand.java"
      provides: "PROJ-02"
    - path: "src/main/java/org/saltations/project/UnregisterProjectCommand.java"
      provides: "PROJ-06"
---

<objective>
Implement `project create`, `project list`, and `project unregister` with ConfigService load/save, validation, and plain-text + `--json` output matching Phase 2 catalog commands. Per `03-CONTEXT.md` D-01, D-03, D-05, D-06, D-08, D-11.
</objective>

<execution_context>
@/home/jmochel/pers/viracocha/.cursor/get-shit-done/workflows/execute-plan.md
@/home/jmochel/pers/viracocha/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-projects-and-mappings/03-CONTEXT.md
@src/main/java/org/saltations/catalog/RegisterCatalogCommand.java
@src/main/java/org/saltations/catalog/ListCatalogsCommand.java
@src/main/java/org/saltations/catalog/UnregisterCatalogCommand.java
@src/main/java/org/saltations/config/ConfigService.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement create and list</name>
  <files>
    src/main/java/org/saltations/project/CreateProjectCommand.java
    src/main/java/org/saltations/project/ListProjectsCommand.java
  </files>

  <read_first>
    - src/main/java/org/saltations/catalog/RegisterCatalogCommand.java
    - src/main/java/org/saltations/catalog/ListCatalogsCommand.java
    - src/main/java/org/saltations/config/ConfigService.java
    - src/main/java/org/saltations/model/ProjectEntry.java
  </read_first>

  <action>
1. **CreateProjectCommand** (per D-01, PROJ-01, D-06):
   - `@Inject ConfigService`, `@Spec CommandSpec spec`
   - Options: `--name` (required), `--path` (required) — same naming style as publisher register
   - Flow: `configService.load()`; if project name already exists (stream anyMatch on `getName()`), print to stderr exactly: `Project '<name>' already exists.` (or match exact string from D-06: `Project '<name>' already exists.` — use single quotes around name as in user decision)
   - Validate workspace path: `Files.exists(Path.of(path))` — if not exist, print `Error: path does not exist: <path>` to stderr (mirror publisher path check)
   - On success: `config.getProjects().add(new ProjectEntry(name, path, new ArrayList<>()))`, `save`, print success line to stdout e.g. `Project '<name>' created.`
   - Catch `ConfigNotInitializedException` and `IOException` like RegisterCatalogCommand

2. **ListProjectsCommand** (PROJ-02, D-03, D-05):
   - Options: `--json` boolean
   - Load config; if `--json`, for each project `ObjectMapper.writeValueAsString(projectEntry)` one line each (like ListCatalogsCommand)
   - Else plain: `printf` with `%-20s  %s%n` for name and path (same pattern as publishers)
  </action>

  <acceptance_criteria>
    - `CreateProjectCommand.java` contains `configService.load()` and `already exists` (or exact D-06 message)
    - `ListProjectsCommand.java` contains `--json` and `ObjectMapper`
    - `mvn -q test` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement unregister + Micronaut tests</name>
  <files>
    src/main/java/org/saltations/project/UnregisterProjectCommand.java
    src/test/java/org/saltations/project/ProjectCommandsTest.java
  </files>

  <read_first>
    - src/main/java/org/saltations/catalog/UnregisterCatalogCommand.java
    - src/test/java/org/saltations/catalog/RegisterCatalogCommandTest.java (or nearest project command test pattern)
  </read_first>

  <action>
1. **UnregisterProjectCommand** (PROJ-06, D-08):
   - Option: `--name` required
   - Load config; remove project with matching name; if none, print `Project '<name>' not found.` to stderr, return 1 (mirror `Publisher 'x' not found.` style from Phase 2)
   - On success: save, print confirmation

2. Add `src/test/java/org/saltations/project/ProjectCommandsTest.java` with `@MicronautTest` and `@TempDir`:
   - Init config in temp dir (reuse pattern from InitCommandTest or publisher tests): set `XDG_CONFIG_HOME` or inject `ConfigService` with test config path if the project uses that pattern
   - Test: create project → list contains name → unregister → list empty
   - Test: duplicate create returns exit 1
   - Test: unregister missing returns exit 1
   - Assert stderr/stdout strings contain expected substrings
  </action>

  <acceptance_criteria>
    - `UnregisterProjectCommand.java` contains `not found` and `removeIf` or equivalent
    - `ProjectCommandsTest.java` exists with at least 3 `@Test` methods
    - `mvn -q test` exits 0
  </acceptance_criteria>
</task>

</tasks>

## PLANNING COMPLETE

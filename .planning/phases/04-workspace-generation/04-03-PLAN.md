---
phase: 04-workspace-generation
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02-PLAN.md
files_modified:
  - src/main/java/org/saltations/ViracochaCommand.java
  - src/main/java/org/saltations/generate/GenerateCommand.java
  - src/test/java/org/saltations/generate/GenerateCommandTest.java
  - src/test/java/org/saltations/ViracochaCommandTest.java
autonomous: true
requirements:
  - GEN-01
  - GEN-06
  - GEN-07
  - GEN-08

must_haves:
  truths:
    - "Stdout contains exactly one summary line matching Generated/Skipped/Failed counts after generate"
    - "`--verbose` prints one line per file action with Created or Skipped or Failed prefix"
    - "`--dry-run` with `--verbose` still prints planned actions without writing (combine with 04-02)"
  artifacts:
    - path: "src/main/java/org/saltations/generate/GenerateCommand.java"
      provides: "picocli entry for vira generate"
      exports: ["GenerateCommand"]
---

<objective>
Wire **`vira generate --project-name <name>`** with `--dry-run` and `--verbose` (GEN-01 CLI surface, GEN-06–GEN-08), delegate to `GeneratorService`, enforce **config initialized** guard like other commands, print summary line **always**, and add Micronaut/picocli integration tests with captured stdout.
</objective>

<execution_context>
@/home/jmochel/pers/viracocha/.cursor/get-shit-done/workflows/execute-plan.md
@/home/jmochel/pers/viracocha/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-workspace-generation/04-CONTEXT.md
@.planning/REQUIREMENTS.md
@src/main/java/org/saltations/project/CreateProjectCommand.java
@src/main/java/org/saltations/generate/GeneratorService.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add GenerateCommand and register on ViracochaCommand</name>
  <files>
    src/main/java/org/saltations/generate/GenerateCommand.java
    src/main/java/org/saltations/ViracochaCommand.java
  </files>

  <read_first>
    - src/main/java/org/saltations/ViracochaCommand.java
    - src/main/java/org/saltations/project/ShowProjectCommand.java
  </read_first>

  <action>
1. Create `GenerateCommand` in `org.saltations.generate`:
   - `@Command(name = "generate", description = "Generate workspace files from project mappings.", mixinStandardHelpOptions = true)`
   - `@Option(names = {"--project-name"}, required = true) String projectName;`
   - `@Option(names = {"--dry-run"}, description = "...") boolean dryRun;`
   - `@Option(names = {"--verbose"}, description = "...") boolean verbose;`
   - Inject `ConfigService` + `GeneratorService`, `@Spec CommandSpec spec`

2. In `call()`:
   - `configService.load()` inside try; on `ConfigNotInitializedException` print message to stderr and return `1` (same string as other commands — grep existing)
   - Invoke `generatorService.generate(projectName, dryRun)` — adjust signature if result object used
   - Print **one** summary line to stdout: exactly `Generated: %d files, Skipped: %d files, Failed: %d files` with **lowercase "files"** after each number as in GEN-06 / `04-CONTEXT.md` D-09
   - If `--verbose`, before summary print one line per action: `Created <path>`, `Skipped <path>`, or `Failed <path>` where path is **relative to workspace root** (D-10)

3. Register `GenerateCommand.class` in `ViracochaCommand` `subcommands` array.

4. Return `0` on success; return `1` if project not found or business error (document message text in tests).
  </action>

  <acceptance_criteria>
    - `grep "GenerateCommand.class" src/main/java/org/saltations/ViracochaCommand.java` exits 0
    - `grep "Generated:" src/main/java/org/saltations/generate/GenerateCommand.java` exits 0
    - `mvn -q -Dtest=ViracochaCommandTest test` exits 0 after implementation (update test if help text lists generate)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Integration tests for summary, verbose, dry-run, config guard</name>
  <files>
    src/test/java/org/saltations/generate/GenerateCommandTest.java
    src/test/java/org/saltations/ViracochaCommandTest.java
  </files>

  <read_first>
    - src/test/java/org/saltations/ViracochaCommandTest.java
    - src/test/java/org/saltations/config/InitCommandTest.java
  </read_first>

  <action>
1. Add `GenerateCommandTest` with `@MicronautTest` and pattern from existing command tests: `@TempDir Path configDir`, `@TempDir Path workspace`, init config via `ConfigService` + write minimal `ViracochaConfig` YAML **or** use init + create project + register pattern (reuse fixtures from `ProjectCommandsTest` if available).

2. Assert stdout **contains** substring `Generated:` and `Skipped:` and `Failed:` on one line (regex or contains).

3. Test `--verbose`: output contains `Created` or `Skipped` line(s) before summary.

4. Test `--dry-run`: no new files under workspace compared to before (or file count unchanged) while summary still printed.

5. Test config not initialized: stderr contains `Config not initialized` (exact project convention).

6. Update `ViracochaCommandTest` if root help now lists `generate`.

7. Run `mvn -q test`.
  </action>

  <acceptance_criteria>
    - `grep -q "Generated:" src/test/java/org/saltations/generate/GenerateCommandTest.java` exits 0
    - `grep -q "verbose\\|Verbose" src/test/java/org/saltations/generate/GenerateCommandTest.java` exits 0
    - `grep -q "dry-run\\|dryRun" src/test/java/org/saltations/generate/GenerateCommandTest.java` exits 0
    - `mvn -q test` exits 0
  </acceptance_criteria>
</task>

</tasks>

## PLANNING COMPLETE

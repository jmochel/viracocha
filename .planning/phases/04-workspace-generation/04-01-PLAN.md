---
phase: 04-workspace-generation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/org/saltations/model/ProjectEntry.java
  - src/main/java/org/saltations/project/CreateProjectCommand.java
  - src/main/java/org/saltations/generate/PathExpander.java
  - src/test/java/org/saltations/generate/PathExpanderTest.java
  - src/test/java/org/saltations/model/ViracochaConfigProjectTypedListTest.java
autonomous: true
requirements:
  - GEN-02
  - GEN-03

must_haves:
  truths:
    - "ProjectEntry has optional project-level parameters map; mapping-level params still override at generation time (verified in 04-02)"
    - "PathExpander expands ${var} in path segment strings using a String map model without leaving literal ${...} when vars are provided"
    - "mvn test exits 0"
  artifacts:
    - path: "src/main/java/org/saltations/generate/PathExpander.java"
      provides: "Freemarker-based expansion for one path segment"
      exports: ["PathExpander"]
---

<objective>
Add **project-level default parameters** to `ProjectEntry` (GEN-02 data model), fix constructors/usages, and implement **PathExpander** with unit tests (GEN-03) before wiring full generation. Follow `04-CONTEXT.md` D-03–D-05: merged model is project defaults then mapping overlay (implementation of merge happens in 04-02; this plan only adds the project-side map and the path utility).
</objective>

<execution_context>
@/home/jmochel/pers/viracocha/.cursor/get-shit-done/workflows/execute-plan.md
@/home/jmochel/pers/viracocha/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-workspace-generation/04-CONTEXT.md
@.planning/REQUIREMENTS.md
@src/main/java/org/saltations/model/MappingEntry.java
@src/main/java/org/saltations/pattern/FreemarkerVariableExtractor.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add project-level parameters to ProjectEntry and update call sites</name>
  <files>
    src/main/java/org/saltations/model/ProjectEntry.java
    src/main/java/org/saltations/project/CreateProjectCommand.java
    src/test/java/org/saltations/model/ViracochaConfigProjectTypedListTest.java
  </files>

  <read_first>
    - src/main/java/org/saltations/model/ProjectEntry.java
    - src/main/java/org/saltations/project/CreateProjectCommand.java
    - src/test/java/org/saltations/model/ViracochaConfigProjectTypedListTest.java
  </read_first>

  <action>
1. In `ProjectEntry.java`, add field `private Map<String, String> parameters = new LinkedHashMap<>();` (same style as `MappingEntry.parameters`). Keep Lombok `@Data`, `@NoArgsConstructor`, `@AllArgsConstructor` — field order should place `parameters` after `mappings` or follow existing field order consistently.

2. Update `CreateProjectCommand.java` line that constructs `ProjectEntry`: pass an empty `new LinkedHashMap<>()` for parameters as the fourth constructor argument (or use no-arg + setters if you refactor — prefer explicit constructor matching `@AllArgsConstructor` field order).

3. Extend `ViracochaConfigProjectTypedListTest.java` (or add assertions) so a round-trip YAML includes `parameters:` under a project with at least one key; after deserialize, `getParameters().get("key")` equals expected value. Prove existing configs without `parameters` still load (empty map).

4. Run `mvn -q test`.
  </action>

  <acceptance_criteria>
    - `grep "Map<String, String> parameters" src/main/java/org/saltations/model/ProjectEntry.java` exits 0
    - `grep "LinkedHashMap" src/main/java/org/saltations/project/CreateProjectCommand.java` exits 0 OR constructor call includes parameters argument
    - `mvn -q test` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement PathExpander + unit tests</name>
  <files>
    src/main/java/org/saltations/generate/PathExpander.java
    src/test/java/org/saltations/generate/PathExpanderTest.java
  </files>

  <read_first>
    - src/main/java/org/saltations/pattern/FreemarkerVariableExtractor.java
    - pom.xml (freemarker version)
  </read_first>

  <action>
1. Create package `org.saltations.generate` and class `PathExpander` with a method such as `String expandSegment(String segment, Map<String, String> model)` that:
   - Uses Freemarker `Configuration` + `StringTemplateLoader` (or equivalent) to treat `segment` as a tiny template and process with `model` as the root map (String values are sufficient).
   - Expands expressions consistent with path segments used in patterns (same `${var}` style as `FreemarkerVariableExtractor` VAR_PATTERN); document if full Freemarker syntax is supported in segments.
   - Returns a string with no remaining `${...}` when all referenced keys exist in `model`; if a key is missing, either fail fast with a clear exception or document behavior — **prefer** clear `IllegalArgumentException` or Freemarker exception with segment text so generation can count Failed (align with 04-02 error handling).

2. Create `PathExpanderTest.java` with JUnit 5:
   - `expandSegment("foo-${bar}-baz", Map.of("bar", "X"))` returns `foo-X-baz`
   - Nested path segment edge case: literal `$` handling as needed
   - At least one test with two variables

3. Run `mvn -q test`.
  </action>

  <acceptance_criteria>
    - `test -f src/main/java/org/saltations/generate/PathExpander.java` exits 0
    - `grep -q "class PathExpanderTest" src/test/java/org/saltations/generate/PathExpanderTest.java` exits 0
    - `mvn -q -Dtest=PathExpanderTest test` exits 0
  </acceptance_criteria>
</task>

</tasks>

## PLANNING COMPLETE

---
phase: 08-model-config-foundation
plan: "02"
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - src/main/java/org/saltations/config/ConfigVersionException.java
  - src/main/java/org/saltations/config/ConfigService.java
  - src/main/java/org/saltations/archetype/ArchetypeCommand.java (DELETE)
  - src/main/java/org/saltations/archetype/ListArchetypesCommand.java (DELETE)
  - src/main/java/org/saltations/archetype/RegisterArchetypeCommand.java (DELETE)
  - src/main/java/org/saltations/archetype/ShowArchetypeCommand.java (DELETE)
  - src/main/java/org/saltations/archetype/UnregisterArchetypeCommand.java (DELETE)
  - src/main/java/org/saltations/catalog/CatalogCommand.java (DELETE)
  - src/main/java/org/saltations/catalog/ListCatalogsCommand.java (DELETE)
  - src/main/java/org/saltations/catalog/RegisterCatalogCommand.java (DELETE)
  - src/main/java/org/saltations/catalog/ShowCatalogCommand.java (DELETE)
  - src/main/java/org/saltations/catalog/UnregisterCatalogCommand.java (DELETE)
  - src/main/java/org/saltations/project/AddMappingCommand.java (DELETE)
  - src/main/java/org/saltations/project/CreateProjectCommand.java (DELETE)
  - src/main/java/org/saltations/project/ListProjectsCommand.java (DELETE)
  - src/main/java/org/saltations/project/ProjectCommand.java (DELETE)
  - src/main/java/org/saltations/project/ShowProjectCommand.java (DELETE)
  - src/main/java/org/saltations/project/UnregisterProjectCommand.java (DELETE)
  - src/main/java/org/saltations/subscription/AddSubscriptionCommand.java (DELETE)
  - src/main/java/org/saltations/subscription/ListSubscriptionsCommand.java (DELETE)
  - src/main/java/org/saltations/subscription/RemoveSubscriptionCommand.java (DELETE)
  - src/main/java/org/saltations/subscription/ShowSubscriptionCommand.java (DELETE)
  - src/main/java/org/saltations/subscription/SubscriptionCommand.java (DELETE)
  - src/main/java/org/saltations/ViracochaCommand.java
  - src/main/resources/META-INF/native-image/org.saltations/viracocha/reflect-config.json
  - src/test/java/org/saltations/catalog/ListCatalogsCommandTest.java (DELETE)
  - src/test/java/org/saltations/catalog/RegisterCatalogCommandTest.java (DELETE)
  - src/test/java/org/saltations/catalog/ShowCatalogCommandTest.java (DELETE)
  - src/test/java/org/saltations/catalog/UnregisterCatalogCommandTest.java (DELETE)
  - src/test/java/org/saltations/project/AddMappingAndShowProjectTest.java (DELETE)
  - src/test/java/org/saltations/project/ProjectCommandsTest.java (DELETE)
  - src/test/java/org/saltations/subscription/AddSubscriptionCommandTest.java (DELETE)
  - src/test/java/org/saltations/subscription/SubscriptionListShowRemoveTest.java (DELETE)
  - src/test/java/org/saltations/sync/DefaultSyncServiceOneWayTest.java (STUB)
  - src/test/java/org/saltations/sync/DefaultSyncServiceBidirectionalTest.java (STUB)
  - src/test/java/org/saltations/sync/SyncCommandIntegrationTest.java (STUB)
  - src/test/java/org/saltations/config/ConfigServiceTest.java
  - src/test/java/org/saltations/config/ShowConfigCommandTest.java
  - src/test/java/org/saltations/ViracochaCommandTest.java
autonomous: true
requirements:
  - CFG-02
  - CFG-03

must_haves:
  truths:
    - "ConfigService.load() throws ConfigVersionException for a config file with version < 3"
    - "ConfigService.load() throws ConfigVersionException for a config file with no version field"
    - "ConfigVersionException message is: 'Config file is v{N} — v3 format required. Delete ~/.config/viracocha/config.yaml and run ''vira config init'' to start fresh.'"
    - "ConfigService.init() writes a config file with version: 3"
    - "vira archetype, vira catalog, vira project, vira subscription produce 'Unknown subcommand' error (picocli exit 2)"
    - "vira --help lists: config, generate, sync — and NOT archetype, catalog, project, subscription"
    - "All archetype/, catalog/, project/, subscription/ command packages deleted from disk"
    - "./mvnw test -q passes with no failures"
  artifacts:
    - path: "src/main/java/org/saltations/config/ConfigVersionException.java"
      provides: "Checked exception thrown by ConfigService.load() for v2 configs"
      contains: "extends IOException"
    - path: "src/main/java/org/saltations/config/ConfigService.java"
      provides: "Updated load() with version guard"
      contains: "ConfigVersionException"
    - path: "src/main/java/org/saltations/ViracochaCommand.java"
      provides: "Updated subcommand list (no v2 commands)"
      contains: "ConfigCommand.class, GenerateCommand.class, SyncCommand.class"
    - path: "src/main/resources/META-INF/native-image/org.saltations/viracocha/reflect-config.json"
      provides: "Updated GraalVM reflect config with v3 model entries only"
      contains: "SourceEntry"
  key_links:
    - from: "src/main/java/org/saltations/config/ConfigService.java"
      to: "src/main/java/org/saltations/config/ConfigVersionException.java"
      via: "throw new ConfigVersionException(version)"
      pattern: "throw new ConfigVersionException"
    - from: "src/main/java/org/saltations/config/ShowConfigCommand.java"
      to: "src/main/java/org/saltations/config/ConfigVersionException.java"
      via: "IOException catch block (ConfigVersionException extends IOException)"
      pattern: "catch.*IOException"
---

<objective>
Add ConfigVersionException and the version guard to ConfigService.load(), delete all four v2 CLI
command packages (archetype, catalog, project, subscription) with their tests, remove the deleted
subcommands from ViracochaCommand, and update reflect-config.json to v3 entries. Stub the sync
test files that reference deleted v2 model classes.

Purpose: Complete CFG-02 (version guard) and CFG-03 (v2 package removal). After this plan,
running any v2 command name produces a picocli "Unknown subcommand" error and a v2 config file
causes a clear version error message on any config-using command.

Output: ConfigVersionException class; updated ConfigService.load(); all four v2 command packages
gone; ViracochaCommand registering only config/generate/sync; updated reflect-config.json;
full test suite green.
</objective>

<execution_context>
@/home/jmochel/pers/viracocha/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmochel/pers/viracocha/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-model-config-foundation/08-CONTEXT.md
@.planning/phases/08-model-config-foundation/08-RESEARCH.md
@.planning/phases/08-model-config-foundation/08-01-SUMMARY.md

<interfaces>
<!-- Key contracts the executor needs. Plan 01 must have completed before Plan 02 begins. -->

From src/main/java/org/saltations/config/ConfigNotInitializedException.java (pattern to follow):
```java
public class ConfigNotInitializedException extends RuntimeException {
    public ConfigNotInitializedException() {
        super("Config not initialized. Run 'vira config init' first.");
    }
}
```

From src/main/java/org/saltations/config/ConfigService.java (current load() — to modify):
```java
public ViracochaConfig load() throws IOException {
    Path configFile = xdgPaths.configFile();
    if (!Files.exists(configFile)) {
        throw new ConfigNotInitializedException();
    }
    return yaml.readValue(configFile.toFile(), ViracochaConfig.class);
}
```

The ObjectMapper yaml field is already an `ObjectMapper(new YAMLFactory())` instance.
`yaml.readTree(File)` is available and returns `JsonNode` without needing a new import
(com.fasterxml.jackson.databind.JsonNode is in jackson-databind already on the classpath).

From UI-SPEC.md — exact error message for version guard (locked D-09):
`Config file is v{N} — v3 format required. Delete ~/.config/viracocha/config.yaml and run 'vira config init' to start fresh.`
Where {N} is the int found in the version field, or 0 when field is absent.

When surfaced via ShowConfigCommand's existing IOException catch:
`Error reading config: Config file is v{N} — v3 format required. Delete ~/.config/viracocha/config.yaml and run 'vira config init' to start fresh.`

reflect-config.json — exact v3 replacement content (per D-11):
```json
[
  { "name": "org.saltations.model.ViracochaConfig",
    "allDeclaredConstructors": true, "allDeclaredFields": true, "allDeclaredMethods": true },
  { "name": "org.saltations.model.SourceEntry",
    "allDeclaredConstructors": true, "allDeclaredFields": true, "allDeclaredMethods": true },
  { "name": "org.saltations.model.DestinationEntry",
    "allDeclaredConstructors": true, "allDeclaredFields": true, "allDeclaredMethods": true },
  { "name": "org.saltations.model.MappingEntry",
    "allDeclaredConstructors": true, "allDeclaredFields": true, "allDeclaredMethods": true }
]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ConfigVersionException and version guard to ConfigService.load()</name>
  <files>
    src/main/java/org/saltations/config/ConfigVersionException.java (CREATE),
    src/main/java/org/saltations/config/ConfigService.java (UPDATE load() and init()),
    src/test/java/org/saltations/config/ConfigServiceTest.java (ADD version guard tests),
    src/test/java/org/saltations/config/ShowConfigCommandTest.java (ADD v2 config test)
  </files>
  <read_first>
    - src/main/java/org/saltations/config/ConfigService.java — current load() and init() to modify
    - src/main/java/org/saltations/config/ConfigNotInitializedException.java — exception pattern to follow
    - src/test/java/org/saltations/config/ConfigServiceTest.java — existing tests; add version guard tests here
    - src/test/java/org/saltations/config/ShowConfigCommandTest.java — add test for v2 config error display
    - src/main/java/org/saltations/config/ShowConfigCommand.java — verify IOException catch will surface message
    - src/main/java/org/saltations/model/ViracochaConfig.java — verify version=3 default is set (from Plan 01)
  </read_first>
  <behavior>
    - ConfigService.load() on a config file containing `version: 1` throws ConfigVersionException
    - ConfigService.load() on a config file missing the version field (treats missing as version=0) throws ConfigVersionException
    - ConfigService.load() on a config file containing `version: 3` returns a ViracochaConfig without throwing
    - ConfigService.init() when config does not exist creates a file containing `version: 3`
    - ShowConfigCommand with a v2 config file prints to stderr a message containing "v3 format required" and returns exit code 1
  </behavior>
  <action>
    STEP 1 — Create ConfigVersionException.java:
    Create src/main/java/org/saltations/config/ConfigVersionException.java:
    ```java
    package org.saltations.config;

    import java.io.IOException;

    /**
     * Thrown by ConfigService.load() when the config file version is less than 3.
     * Extends IOException so it is caught by existing IOException handlers in commands
     * (e.g. ShowConfigCommand) and the message propagates to stderr as-is.
     * Commands must not catch this separately — the IOException handler covers it.
     */
    public class ConfigVersionException extends IOException {

        public ConfigVersionException(int foundVersion) {
            super("Config file is v" + foundVersion
                + " — v3 format required."
                + " Delete ~/.config/viracocha/config.yaml"
                + " and run 'vira config init' to start fresh.");
        }
    }
    ```

    NOTE: The em-dash character (—) in the message MUST be the Unicode character U+2014 written as
    the Java Unicode escape — to avoid file encoding issues. Do NOT use a literal em-dash character
    in the source file.

    STEP 2 — Update ConfigService.load():
    Add the version pre-read immediately after the file existence check and before the full deserialization.
    The updated load() method must be:

    ```java
    public ViracochaConfig load() throws IOException {
        Path configFile = xdgPaths.configFile();
        if (!Files.exists(configFile)) {
            throw new ConfigNotInitializedException();
        }
        // Version pre-read: detect v2 config before full deserialization (per D-09)
        com.fasterxml.jackson.databind.JsonNode root = yaml.readTree(configFile.toFile());
        com.fasterxml.jackson.databind.JsonNode versionNode = root.get("version");
        int version = (versionNode == null || versionNode.isNull()) ? 0 : versionNode.asInt(0);
        if (version < 3) {
            throw new ConfigVersionException(version);
        }
        return yaml.readValue(configFile.toFile(), ViracochaConfig.class);
    }
    ```

    Add the import at the top of ConfigService.java:
    `import com.fasterxml.jackson.databind.JsonNode;`
    Then use `JsonNode` (short form) in the load() method body instead of fully-qualified names.

    STEP 3 — Verify ConfigService.init() behavior:
    Read the existing init() method. It calls `save(new ViracochaConfig())`. Since Plan 01 updated
    ViracochaConfig to have `version = 3` as default, init() already writes version:3. No code
    change to init() is needed — only verify this is correct.

    STEP 4 — Add version guard tests to ConfigServiceTest:
    Read ConfigServiceTest.java. Append these test methods to the existing class:

    ```java
    @Test
    void loadThrowsConfigVersionExceptionForV1Config() throws IOException {
        // Write a v1 config file manually (simulates a developer's existing v2 config)
        Path configDir = configService.xdgPaths().configDir();
        Files.createDirectories(configDir);
        Path configFile = configService.xdgPaths().configFile();
        Files.writeString(configFile,
            "version: 1\ncatalogs: []\narchetypes: []\nprojects: []\n");
        ConfigVersionException ex = assertThrows(ConfigVersionException.class,
            () -> configService.load(),
            "load() must throw ConfigVersionException when version is 1");
        assertTrue(ex.getMessage().contains("v3 format required"),
            "Exception message must contain 'v3 format required'");
        assertTrue(ex.getMessage().contains("v1"),
            "Exception message must report the found version (v1)");
    }

    @Test
    void loadThrowsConfigVersionExceptionForMissingVersionField() throws IOException {
        Path configDir = configService.xdgPaths().configDir();
        Files.createDirectories(configDir);
        Path configFile = configService.xdgPaths().configFile();
        Files.writeString(configFile, "catalogs: []\n"); // no version field
        ConfigVersionException ex = assertThrows(ConfigVersionException.class,
            () -> configService.load(),
            "load() must throw ConfigVersionException when version field is absent");
        assertTrue(ex.getMessage().contains("v0"),
            "Exception message must report version 0 for missing field");
    }

    @Test
    void loadSucceedsForV3Config() throws IOException {
        configService.init();
        // init() writes version:3; this must load without exception
        ViracochaConfig config = assertDoesNotThrow(() -> configService.load());
        assertEquals(3, config.getVersion());
    }

    @Test
    void initWritesVersion3() throws IOException {
        Path path = configService.init();
        String contents = Files.readString(path);
        assertTrue(contents.contains("version: 3"),
            "init() must write a config file containing 'version: 3'");
    }
    ```

    STEP 5 — Read ShowConfigCommandTest to understand test structure:
    Read src/test/java/org/saltations/config/ShowConfigCommandTest.java.
    Add a test for v2 config behavior. The pattern to add:

    ```java
    @Test
    void showPrintsVersionErrorForV2Config() throws Exception {
        // Write a v2 (version:1) config file
        Path configDir = configService.xdgPaths().configDir();
        Files.createDirectories(configDir);
        Path configFile = configService.xdgPaths().configFile();
        Files.writeString(configFile, "version: 1\ncatalogs: []\n");

        // Execute show command — should fail with version error
        ByteArrayOutputStream stderr = new ByteArrayOutputStream();
        // [read ShowConfigCommandTest setUp to use the same CommandLine setup pattern]
        // The test must:
        // 1. Execute the show command against the v1 config
        // 2. Assert exit code is 1
        // 3. Assert stderr contains "v3 format required"
    }
    ```

    IMPORTANT: Read the full ShowConfigCommandTest.java before writing this test. Follow the exact
    setUp pattern used in that test file to build the CommandLine and capture stderr. Do NOT guess
    at the test setup — read the file first.

    STEP 6 — Run targeted tests:
    ./mvnw test -Dtest=ConfigServiceTest,ShowConfigCommandTest -q
    All tests must pass before completing this task.
  </action>
  <verify>
    <automated>./mvnw test -Dtest=ConfigServiceTest,ShowConfigCommandTest -q</automated>
  </verify>
  <done>
    - ConfigVersionException.java exists in org.saltations.config, extends IOException
    - ConfigVersionException message contains "v{N} — v3 format required. Delete ~/.config/viracocha/config.yaml and run 'vira config init' to start fresh."
    - ConfigService.load() throws ConfigVersionException for version=1 config
    - ConfigService.load() throws ConfigVersionException for config with no version field (treats as v0)
    - ConfigService.load() does NOT throw for version=3 config
    - ConfigServiceTest passes with 4 new version guard tests
    - ShowConfigCommandTest passes with v2 config error test
    - ./mvnw test -Dtest=ConfigServiceTest,ShowConfigCommandTest -q exits 0
  </done>
  <acceptance_criteria>
    - grep "extends IOException" src/main/java/org/saltations/config/ConfigVersionException.java returns a match
    - grep "v3 format required" src/main/java/org/saltations/config/ConfigVersionException.java returns a match
    - grep "readTree" src/main/java/org/saltations/config/ConfigService.java returns a match
    - grep "ConfigVersionException" src/main/java/org/saltations/config/ConfigService.java returns a match (at least 1 throw)
    - grep "loadThrowsConfigVersionException" src/test/java/org/saltations/config/ConfigServiceTest.java returns a match
    - ./mvnw test -Dtest=ConfigServiceTest -q exits 0
    - ./mvnw test -Dtest=ShowConfigCommandTest -q exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Delete v2 command packages, update ViracochaCommand, update reflect-config.json, stub sync tests</name>
  <files>
    src/main/java/org/saltations/archetype/ (DELETE all 5 remaining command files),
    src/main/java/org/saltations/catalog/ (DELETE all 5 files),
    src/main/java/org/saltations/project/ (DELETE all 6 files),
    src/main/java/org/saltations/subscription/ (DELETE all 5 files),
    src/main/java/org/saltations/ViracochaCommand.java (UPDATE subcommands),
    src/main/resources/META-INF/native-image/org.saltations/viracocha/reflect-config.json (REPLACE),
    src/test/java/org/saltations/catalog/ (DELETE all 4 test files),
    src/test/java/org/saltations/project/ (DELETE 2 test files),
    src/test/java/org/saltations/subscription/ (DELETE 2 test files),
    src/test/java/org/saltations/sync/DefaultSyncServiceOneWayTest.java (STUB),
    src/test/java/org/saltations/sync/DefaultSyncServiceBidirectionalTest.java (STUB),
    src/test/java/org/saltations/sync/SyncCommandIntegrationTest.java (STUB),
    src/test/java/org/saltations/ViracochaCommandTest.java (UPDATE assertions)
  </files>
  <read_first>
    - src/main/java/org/saltations/ViracochaCommand.java — current subcommands list to update
    - src/main/resources/META-INF/native-image/org.saltations/viracocha/reflect-config.json — current v2 entries to replace
    - src/test/java/org/saltations/sync/DefaultSyncServiceOneWayTest.java — imports v2 classes; stub it
    - src/test/java/org/saltations/sync/DefaultSyncServiceBidirectionalTest.java — imports v2 classes; stub it
    - src/test/java/org/saltations/sync/SyncCommandIntegrationTest.java — imports v2 classes; stub it
    - src/test/java/org/saltations/ViracochaCommandTest.java — asserts "subscription" subcommand; must update
  </read_first>
  <action>
    === PHASE A: Delete remaining archetype command files ===

    The archetype package still has 5 command files left (ArchetypePathUtils and
    FreemarkerVariableExtractor were already deleted by Plan 01 Task 2 Phase I):
    Delete:
    - src/main/java/org/saltations/archetype/ArchetypeCommand.java
    - src/main/java/org/saltations/archetype/ListArchetypesCommand.java
    - src/main/java/org/saltations/archetype/RegisterArchetypeCommand.java
    - src/main/java/org/saltations/archetype/ShowArchetypeCommand.java
    - src/main/java/org/saltations/archetype/UnregisterArchetypeCommand.java

    NOTE: If ArchetypePathUtils.java or FreemarkerVariableExtractor.java still exist in archetype/
    (in case Plan 01 Task 2 did not delete them), delete them here too.

    Run: ./mvnw compile -q
    If any compile error references org.saltations.archetype, diagnose and fix before continuing.

    === PHASE B: Delete catalog command package ===

    Delete:
    - src/main/java/org/saltations/catalog/CatalogCommand.java
    - src/main/java/org/saltations/catalog/ListCatalogsCommand.java
    - src/main/java/org/saltations/catalog/RegisterCatalogCommand.java
    - src/main/java/org/saltations/catalog/ShowCatalogCommand.java
    - src/main/java/org/saltations/catalog/UnregisterCatalogCommand.java

    Run: ./mvnw compile -q
    If any compile error, diagnose. Expected error: ViracochaCommand still imports CatalogCommand.
    That is OK — ViracochaCommand is fixed in PHASE E. If the only errors are in ViracochaCommand,
    proceed to PHASE C.

    === PHASE C: Delete project command package ===

    Delete:
    - src/main/java/org/saltations/project/AddMappingCommand.java
    - src/main/java/org/saltations/project/CreateProjectCommand.java
    - src/main/java/org/saltations/project/ListProjectsCommand.java
    - src/main/java/org/saltations/project/ProjectCommand.java
    - src/main/java/org/saltations/project/ShowProjectCommand.java
    - src/main/java/org/saltations/project/UnregisterProjectCommand.java

    Run: ./mvnw compile -q
    Same expectation — errors will be in ViracochaCommand only.

    === PHASE D: Delete subscription command package ===

    Delete:
    - src/main/java/org/saltations/subscription/AddSubscriptionCommand.java
    - src/main/java/org/saltations/subscription/ListSubscriptionsCommand.java
    - src/main/java/org/saltations/subscription/RemoveSubscriptionCommand.java
    - src/main/java/org/saltations/subscription/ShowSubscriptionCommand.java
    - src/main/java/org/saltations/subscription/SubscriptionCommand.java

    Run: ./mvnw compile -q
    Same expectation.

    === PHASE E: Update ViracochaCommand (per D-12) ===

    Replace src/main/java/org/saltations/ViracochaCommand.java. The new version:
    1. Removes imports for: ArchetypeCommand, CatalogCommand, ProjectCommand, SubscriptionCommand
    2. Retains imports for: ConfigCommand, GenerateCommand, SyncCommand (and picocli/Micronaut imports)
    3. Updates @Command subcommands = {...} to contain ONLY:
       ConfigCommand.class, GenerateCommand.class, SyncCommand.class
    4. Class body (Callable<Integer> implementation) is unchanged

    New ViracochaCommand.java:
    ```java
    package org.saltations;

    import io.micronaut.configuration.picocli.PicocliRunner;
    import jakarta.inject.Singleton;
    import org.saltations.config.ConfigCommand;
    import org.saltations.generate.GenerateCommand;
    import org.saltations.sync.SyncCommand;
    import picocli.CommandLine.Command;

    import java.util.concurrent.Callable;

    /**
     * Root viracocha CLI command.
     * Entry point: main() wires System.exit() to the picocli execute() return value
     * so exit codes from Callable&lt;Integer&gt; are propagated to the shell.
     */
    @Command(
        name = "vira",
        description = {
            "Workspace manager for AI-assisted development."
        },
        mixinStandardHelpOptions = true,
        subcommands = {
            ConfigCommand.class,
            GenerateCommand.class,
            SyncCommand.class
        }
    )
    @Singleton
    public class ViracochaCommand implements Callable<Integer> {

        public static void main(String[] args) throws Exception {
            System.exit(PicocliRunner.execute(ViracochaCommand.class, args));
        }

        @Override
        public Integer call() {
            return 0;
        }
    }
    ```

    Run: ./mvnw compile -q
    This MUST succeed with zero errors. If it fails, diagnose — common causes:
    - Leftover import of a deleted command class (check ViracochaCommand.java)
    - A remaining reference in another file (grep for package names)

    === PHASE F: Update reflect-config.json (per D-11) ===

    Replace the entire content of:
    src/main/resources/META-INF/native-image/org.saltations/viracocha/reflect-config.json

    New content (exact):
    ```json
    [
      {
        "name": "org.saltations.model.ViracochaConfig",
        "allDeclaredConstructors": true,
        "allDeclaredFields": true,
        "allDeclaredMethods": true
      },
      {
        "name": "org.saltations.model.SourceEntry",
        "allDeclaredConstructors": true,
        "allDeclaredFields": true,
        "allDeclaredMethods": true
      },
      {
        "name": "org.saltations.model.DestinationEntry",
        "allDeclaredConstructors": true,
        "allDeclaredFields": true,
        "allDeclaredMethods": true
      },
      {
        "name": "org.saltations.model.MappingEntry",
        "allDeclaredConstructors": true,
        "allDeclaredFields": true,
        "allDeclaredMethods": true
      }
    ]
    ```

    === PHASE G: Delete v2 command test directories ===

    Delete all files in these test directories:
    - src/test/java/org/saltations/catalog/ (4 files: ListCatalogsCommandTest, RegisterCatalogCommandTest, ShowCatalogCommandTest, UnregisterCatalogCommandTest)
    - src/test/java/org/saltations/project/ (2 files: AddMappingAndShowProjectTest, ProjectCommandsTest)
    - src/test/java/org/saltations/subscription/ (2 files: AddSubscriptionCommandTest, SubscriptionListShowRemoveTest)

    === PHASE H: Stub sync test files ===

    The sync test files reference deleted v2 model classes (ProjectEntry, CatalogEntry,
    SubscriptionEntry, SubscriptionSyncDirection). Replace each with a minimal stub.

    Replace src/test/java/org/saltations/sync/DefaultSyncServiceOneWayTest.java:
    ```java
    package org.saltations.sync;

    import org.junit.jupiter.api.Test;
    import static org.junit.jupiter.api.Assertions.assertTrue;

    /**
     * Stub — DefaultSyncService tests rewritten in Phase 12 for v3 source/destination model.
     */
    class DefaultSyncServiceOneWayTest {
        @Test
        void placeholder() {
            assertTrue(true, "DefaultSyncService one-way tests rewritten in Phase 12");
        }
    }
    ```

    Replace src/test/java/org/saltations/sync/DefaultSyncServiceBidirectionalTest.java:
    ```java
    package org.saltations.sync;

    import org.junit.jupiter.api.Test;
    import static org.junit.jupiter.api.Assertions.assertTrue;

    /**
     * Stub — DefaultSyncService bidirectional tests rewritten in Phase 12 for v3 model.
     */
    class DefaultSyncServiceBidirectionalTest {
        @Test
        void placeholder() {
            assertTrue(true, "DefaultSyncService bidirectional tests rewritten in Phase 12");
        }
    }
    ```

    Replace src/test/java/org/saltations/sync/SyncCommandIntegrationTest.java:
    ```java
    package org.saltations.sync;

    import org.junit.jupiter.api.Test;
    import static org.junit.jupiter.api.Assertions.assertTrue;

    /**
     * Stub — SyncCommand integration tests rewritten in Phase 12 for v3 model.
     */
    class SyncCommandIntegrationTest {
        @Test
        void placeholder() {
            assertTrue(true, "SyncCommand integration tests rewritten in Phase 12");
        }
    }
    ```

    === PHASE I: Update ViracochaCommandTest ===

    Read src/test/java/org/saltations/ViracochaCommandTest.java.

    The current test asserts that `--help` output contains "subscription". That assertion must be
    REMOVED (subscription command no longer exists). Update the test to:
    1. Remove: `assertTrue(help.contains("subscription"), ...)` assertion
    2. Add: `assertFalse(help.contains("subscription"), "--help must NOT list removed 'subscription' subcommand")`
    3. Add: `assertFalse(help.contains("archetype"), "--help must NOT list removed 'archetype' subcommand")`
    4. Add: `assertFalse(help.contains("catalog"), "--help must NOT list removed 'catalog' subcommand")`
    5. Add: `assertFalse(help.contains("project"), "--help must NOT list removed 'project' subcommand")`
    6. Keep: the existing assertTrue assertions for "config", "generate", "sync"

    === PHASE J: Run full test suite ===

    Run: ./mvnw test -q
    All tests must pass. If any fail, diagnose and fix before completing this task.

    If ViracochaCommandTest fails because the ApplicationContext cannot find beans for deleted
    command classes (e.g. Micronaut scanning classpath), verify that the deleted class files are
    not still present in target/. Run `./mvnw clean test -q` if needed.
  </action>
  <verify>
    <automated>./mvnw test -q</automated>
  </verify>
  <done>
    - All 4 v2 command packages deleted from src/main/java/ (archetype, catalog, project, subscription)
    - ViracochaCommand.java subcommands = {ConfigCommand.class, GenerateCommand.class, SyncCommand.class}
    - ViracochaCommand.java has NO imports for ArchetypeCommand, CatalogCommand, ProjectCommand, SubscriptionCommand
    - reflect-config.json contains entries for ViracochaConfig, SourceEntry, DestinationEntry, MappingEntry only
    - All v2 command test directories deleted (catalog/, project/, subscription/)
    - Sync test files are minimal stubs
    - ViracochaCommandTest asserts "subscription", "archetype", "catalog", "project" are NOT in --help output
    - ./mvnw test -q exits 0
  </done>
  <acceptance_criteria>
    - test -d src/main/java/org/saltations/archetype returns false (directory deleted)
    - test -d src/main/java/org/saltations/catalog returns false (directory deleted)
    - test -d src/main/java/org/saltations/project returns false (directory deleted)
    - test -d src/main/java/org/saltations/subscription returns false (directory deleted)
    - grep "subcommands" src/main/java/org/saltations/ViracochaCommand.java contains ConfigCommand, GenerateCommand, SyncCommand but NOT ArchetypeCommand, CatalogCommand, ProjectCommand, SubscriptionCommand
    - grep "ArchetypeCommand\|CatalogCommand\|ProjectCommand\|SubscriptionCommand" src/main/java/org/saltations/ViracochaCommand.java returns no results
    - grep "SourceEntry" src/main/resources/META-INF/native-image/org.saltations/viracocha/reflect-config.json returns a match
    - grep "CatalogEntry\|ArchetypeEntry\|ProjectEntry" src/main/resources/META-INF/native-image/org.saltations/viracocha/reflect-config.json returns no results
    - grep "subscription" src/test/java/org/saltations/ViracochaCommandTest.java does NOT appear in a assertTrue (assertFalse only)
    - ./mvnw test -q exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
After both tasks complete:

1. `./mvnw test -q` exits 0 — full test suite green
2. `grep -rn "CatalogEntry\|ArchetypeEntry\|ProjectEntry\|SubscriptionEntry\|SubscriptionSyncDirection" src/main/java/` returns no results
3. `grep -rn "org.saltations.archetype\|org.saltations.catalog\|org.saltations.project\|org.saltations.subscription" src/main/java/` returns no results
4. `grep "ConfigVersionException" src/main/java/org/saltations/config/ConfigService.java` returns a match
5. `grep "SourceEntry" src/main/resources/META-INF/native-image/org.saltations/viracocha/reflect-config.json` returns a match
6. `ls src/main/java/org/saltations/` shows only: ViracochaCommand.java, config/, generate/, infra/, model/, sync/
</verification>

<success_criteria>
- ConfigVersionException.java exists and extends IOException with the exact D-09 message
- ConfigService.load() throws ConfigVersionException for version < 3 or missing version field
- ConfigService.load() succeeds for version = 3
- ConfigServiceTest has 4 new version guard tests; all pass
- ShowConfigCommandTest has v2 config error test; passes
- All 4 v2 command packages deleted from production and test source trees
- ViracochaCommand subcommands list: ConfigCommand, GenerateCommand, SyncCommand only
- reflect-config.json contains v3 model entries (ViracochaConfig, SourceEntry, DestinationEntry, MappingEntry)
- ViracochaCommandTest updated to assert v2 commands are absent from --help output
- ./mvnw test -q exits 0 with no failures
- Phase 8 CFG-01 (v3 POJOs, round-trip) covered by Plan 01
- Phase 8 CFG-02 (version guard) covered by Plan 02 Task 1
- Phase 8 CFG-03 (v2 package removal) covered by Plan 02 Task 2
</success_criteria>

<output>
After completion, create `.planning/phases/08-model-config-foundation/08-02-SUMMARY.md`
</output>
